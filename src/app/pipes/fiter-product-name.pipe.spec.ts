import { FiterProductNamePipe } from './fiter-product-name.pipe';

describe('FiterProductNamePipe', () => {
  it('create an instance', () => {
    const pipe = new FiterProductNamePipe();
    expect(pipe).toBeTruthy();
  });
});
